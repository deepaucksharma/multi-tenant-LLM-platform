# Lightweight CI dependencies (no CUDA/GPU)
# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Core dependencies
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyyaml>=6.0
loguru>=0.7.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# NLP
nltk>=3.8
rouge-score>=0.1.2

# Data processing
python-docx>=1.1.0
pypdf>=4.0.0

# API (for module tests)
fastapi>=0.109.0
sse-starlette>=1.8.0

# Database
sqlite3; python_version >= '3.0'  # Built-in, but explicit for clarity

# HF Jobs launcher tests / tooling
huggingface-hub>=1.8.0
