# Lightweight requirements for HF Spaces deployment.
# Omits torch/CUDA — generation is handled by HF Serverless Inference API.

# API / Backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.8.0
pydantic>=2.6.0
python-dotenv
httpx
aiofiles
websockets

# RAG / Vector DB (used for retrieval; embedding runs on CPU)
chromadb>=0.4.22
sentence-transformers>=2.5.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
nltk
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# HF Hub (adapter/dataset management)
huggingface-hub>=0.21.0

# Utilities
loguru
jinja2
psutil
pyyaml
